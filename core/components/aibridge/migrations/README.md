# Migrations

The SQL migration files are retained as a development/reference representation.

For the current MODX 3 Extra installation path, the authoritative initial schema installation is the xPDO 3 schema plus the Transport Package resolver. Future upgrade migrations will be introduced here when schema evolution requires them.
