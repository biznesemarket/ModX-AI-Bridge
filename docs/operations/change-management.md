# Change Management Operations

Use the Manager Operations Console to inspect change requests and approvals. Never update workflow status with SQL in production except during documented disaster recovery.

A failed execution is represented separately from rejection. Failed changes require investigation before retry.
