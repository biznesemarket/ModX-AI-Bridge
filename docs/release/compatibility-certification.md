# Compatibility Certification

The supported target is MODX 3.2.x. The primary CI target is MODX 3.2.2-pl. Other 3.2.x versions remain `Verify` until a runtime test is recorded for that exact version.

A compatibility row may be marked Stable only when the complete mandatory release gate passes against the exact MODX/PHP/database combination.
