# Stable Release — 0.1.2

Release: `aibridge-0.1.2.transport.zip`
Tag: `v0.1.2`
Built by: `scripts/release-candidate.php` (invoked in the MODX container with `MODX_ROOT=/var/www/html`)

## Highlights

- **Deterministic MODX provisioning.** `docker/modx/entrypoint.sh` writes `/var/www/html/.modx-ready` only
  after the file tree is fully materialized, and `scripts/test-modx.sh` waits for that marker instead of
  `config.core.php`, which appears mid-`cp -a` and raced the CLI install (`setup/config.xml: No such file or
  directory`).
- **TypeScript SDK runtime tests.** `sdk/typescript/tests/runtime/client.test.mjs` exercises the compiled
  client against a fake `fetch` (auth/JSON headers, idempotency keys, `updateResource` id merge, error mapping,
  `waitForJob` polling/timeout, MCP JSON-RPC envelopes); `npm test` runs them with `node --test`, no new
  dependencies.
- **Supply-chain pinning.** Container images are referenced by digest (`php:8.2-apache`, `composer:2`,
  `mysql:8.0`) and every workflow `uses:` by full commit SHA, with `.github/dependabot.yml` for weekly
  updates; `scripts/verify-supply-chain-pins.sh` enforces the invariant inside `quality-gate.sh`, and the
  repository has `sha_pinning_required=true`.
- **Version identity** bumped to `0.1.2` across package config, runtime responses (`/health`, MCP serverInfo,
  readiness/console defaults) and both SDKs.

## Certification

Pre-release certification on `main`, commit `ad92ab5`, run `34748277073`: `verify` PASS (incl.
`SUPPLY CHAIN PINS: PASS`), `certify` PASS, `package` PASS; artifact `b1b985bf…` (size 139544).

The repository `README.md` is embedded into the transport package manifest, and recording the release updates
README to `0.1.2`. The published artifact is therefore rebuilt from the release commit `c8ac594` and
re-certified by the tag run `34748489085`, which also passed the full gate:

```text
TYPESCRIPT SDK: PASS
SUPPLY CHAIN PINS: PASS
PACKAGE REPRODUCIBILITY: PASS (aibridge-0.1.2, sha256 eedd5f64...)
== MODX INTEGRATION: PASS ==
STABLE certification gates passed.
```

A local rebuild of `c8ac594` reproduces `eedd5f64…` byte-for-byte. The `MODX Integration` job installs
`aibridge-0.1.2.transport.zip` (Signature: `aibridge-0.1.2`); after install all 19 `aibridge_*` rows exist in
`modx_system_settings`, and full PHPUnit is `OK (123 tests, 626 assertions)`.

## Recorded metadata

| field | value |
| --- | --- |
| component | modx-ai-bridge |
| version | 0.1.2 |
| release | (none — Stable) |
| package | aibridge-0.1.2.transport.zip |
| sha256 | `eedd5f643fe97b378f5a4304fb3296b61a88db045d78302887417d88f8cb582d` |
| size_bytes | 139585 |
| signed | false (no signing key; integrity via SHA-256) |
| reproducible | yes (local build and CI artifact hash identically) |
| modx_version | 3.2.2-pl |
| php_version | 8.2.33 |
| migration_level | 001_initial.sql, 002_approval_workflow.sql |

Evidence artifacts: `release-evidence-ad92ab5…` and `release-evidence-c8ac594…` (90 days). Reproduce with
`MODX_ROOT=/var/www/html php scripts/verify-package-reproducibility.php`.

## Status

**Stable.** Supersedes `0.1.1` (`docs/release/0.1.1.md`) and `0.1.0`. Evidence:
`docs/release/stable-status.md`, `docs/testing/iteration-48-release-0.1.2.md`,
`docs/testing/iteration-45-provisioning-readiness.md`, `docs/testing/iteration-46-typescript-runtime-tests.md`,
`docs/testing/iteration-47-supply-chain-pinning.md`, `docs/testing/STATUS.md`.
