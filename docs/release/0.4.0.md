# Stable Release — 0.4.0

Release: `aibridge-0.4.0.transport.zip`
Tag: `v0.4.0`
Built by: `scripts/release-candidate.php` (invoked in the MODX container with `MODX_ROOT=/var/www/html`)

## Highlights

- **Filtered, paginated resource list.** `GET /api/ai/v2/resources` (operation `resource.list`, scope
  `resource:read`) returns a summary projection of non-deleted resources plus `count`, `total`, `limit` and
  `offset`. Whitelisted filters: `parent`, `template`, `context_key`, `published`, `q`
  (pagetitle/alias/description match), `limit` (1..100, default 25), `offset`, `sort`, `dir`; invalid values
  return `400 invalid_filter`. The same operation backs the `resource_list` MCP tool, `listResources()` in both
  SDKs, and a `resource.list` capability entry.
- **Summary list projection.** The listing returns metadata/timestamps only (no `content` or TVs), so responses
  stay bounded; full state remains on `GET /api/ai/v2/resources/{id}`.
- Additive minor over `0.3.0`: no new scope, route-contract break, schema, migration or setting; existing tokens
  with `resource:read` gain list access.

## Certification

`Release` dispatch on `main`, commit `07f3c72`, run `34758755913`: `verify` PASS (incl.
`SUPPLY CHAIN PINS: PASS`), `certify` PASS, `package` PASS.

```text
Transport Package installation: PASS (Signature: aibridge-0.4.0)
MODX runtime verification: PASS
OK (135 tests, 700 assertions)
== 10b. TypeScript SDK live HTTP E2E ==     TYPESCRIPT SDK LIVE HTTP: PASS
PACKAGE REPRODUCIBILITY: PASS (aibridge-0.4.0, sha256 778d061a4c013f19c06e04814f5edd42fbab76f993864dd59634bffc417c864e)
STABLE certification gates passed.
```

After install all 20 `aibridge_*` rows exist in `modx_system_settings` (`aibridge_version = 0.4.0`), and the
full PHPUnit run is `OK (135 tests, 700 assertions)`.

## Recorded metadata

| field | value |
| --- | --- |
| component | modx-ai-bridge |
| version | 0.4.0 |
| release | (none — Stable) |
| package | aibridge-0.4.0.transport.zip |
| sha256 | `778d061a4c013f19c06e04814f5edd42fbab76f993864dd59634bffc417c864e` |
| size_bytes | 143830 |
| signed | false (no signing key; integrity via SHA-256) |
| reproducible | yes (local build and CI artifact hash identically) |
| modx_version | 3.2.2-pl |
| php_version | 8.2.33 |
| migration_level | 001_initial.sql, 002_approval_workflow.sql |

Evidence artifact: `release-evidence-07f3c729766b2f06f1422e8074d52fcfe95b6eac` (90 days). Reproduce with
`MODX_ROOT=/var/www/html php scripts/verify-package-reproducibility.php`.

## Status

**Stable.** Supersedes `0.3.0` (`docs/release/0.3.0.md`), `0.2.0`, `0.1.3`, `0.1.2`, `0.1.1` and `0.1.0`.
Evidence: `docs/release/stable-status.md`, `docs/testing/iteration-54-resource-list.md`,
`docs/testing/STATUS.md`.
