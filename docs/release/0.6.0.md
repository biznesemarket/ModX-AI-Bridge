# Stable Release — 0.6.0

Release: `aibridge-0.6.0.transport.zip`
Tag: `v0.6.0`
Built by: `scripts/release-candidate.php` (invoked in the MODX container with `MODX_ROOT=/var/www/html`)

## Highlights

- **Template-variable filter for the resource list.** `GET /api/ai/v2/resources` and the `resource_list` MCP
  tool accept `tv_name` (resources with an explicit value for that TV) and optional `tv_value` (LIKE match).
  `tv_value` without `tv_name`, or an unknown `tv_name`, returns `400 invalid_filter`; a known TV with no
  matches returns an empty result. Values are trimmed, length-capped and LIKE-escaped.
- Additive minor over `0.5.0`: two new query parameters on the existing `resource.list` / `resource:read`
  operation; no new scope, schema, migration or setting.
- Only explicit per-resource TV overrides are matched (not a TV `default_text`), mirroring the manager search
  behaviour.
- xPDO note: the implementation avoids partial `select()` on collection queries (a missing PK triggers an
  unfiltered lazy load and memory exhaustion) — see `docs/testing/iteration-56-list-tv-filter.md`.

## Certification

`Release` dispatch on `main`, commit `bb3f621`, run `34762964883`: `verify` PASS (incl.
`SUPPLY CHAIN PINS: PASS`), `certify` PASS, `package` PASS.

```text
Transport Package installation: PASS (Signature: aibridge-0.6.0)
MODX runtime verification: PASS
OK (139 tests, 732 assertions)
== 10b. TypeScript SDK live HTTP E2E ==     TYPESCRIPT SDK LIVE HTTP: PASS
PACKAGE REPRODUCIBILITY: PASS (aibridge-0.6.0, sha256 19c1398bd4461b24df660ea87db41df365dec07a7b133ce0c4bad18bce53808d)
STABLE certification gates passed.
```

After install all 20 `aibridge_*` rows exist in `modx_system_settings` (`aibridge_version = 0.6.0`), and the
full PHPUnit run is `OK (139 tests, 732 assertions)`.

## Recorded metadata

| field | value |
| --- | --- |
| component | modx-ai-bridge |
| version | 0.6.0 |
| release | (none — Stable) |
| package | aibridge-0.6.0.transport.zip |
| sha256 | `19c1398bd4461b24df660ea87db41df365dec07a7b133ce0c4bad18bce53808d` |
| size_bytes | 145059 |
| signed | false (no signing key; integrity via SHA-256) |
| reproducible | yes (local build and CI artifact hash identically) |
| modx_version | 3.2.2-pl |
| php_version | 8.2.33 |
| migration_level | 001_initial.sql, 002_approval_workflow.sql |

Evidence artifact: `release-evidence-bb3f6212faf239bf9734445b709da8e9afdad4fa` (90 days). Reproduce with
`MODX_ROOT=/var/www/html php scripts/verify-package-reproducibility.php`.

## Status

**Stable.** Supersedes `0.5.0` (`docs/release/0.5.0.md`), `0.4.0`, `0.3.0`, `0.2.0`, `0.1.3`, `0.1.2`, `0.1.1`
and `0.1.0`. Evidence: `docs/release/stable-status.md`,
`docs/testing/iteration-56-list-tv-filter.md`, `docs/testing/STATUS.md`.
