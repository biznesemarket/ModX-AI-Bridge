# Stable Release — 0.7.0

Release: `aibridge-0.7.0.transport.zip`
Tag: `v0.7.0`
Built by: `scripts/release-candidate.php` (invoked in the MODX container with `MODX_ROOT=/var/www/html`)

## Highlights

- **Contexts in `/capabilities`.** The manifest now advertises the available MODX contexts (`key`, `name`,
  `description`) so clients know the valid `context_key` filter values for `GET /resources`.
- **Join-based TV filter.** The `tv_name`/`tv_value` list filter now matches an explicit
  `modTemplateVarResource` value through a join instead of materializing `contentid`s and applying `id:IN`, so
  `count`/`total` stay a single aggregate query. Public filter contract and response are unchanged.
- **`publishedon` sort** is confirmed as supported and is now covered by an integration test.
- Additive minor over `0.6.0`: no new scope, route, schema, migration or setting.

## Certification

`Release` dispatch on `main`, commit `82e9917`, run `34764596898` (rerun after a MODX-readiness flake in the
first attempt: the entrypoint did not create `/var/www/html/.modx-ready` at step 3, before this code was
installed; `gh run rerun --failed` then passed): `verify` PASS (incl. `SUPPLY CHAIN PINS: PASS`), `certify` PASS,
`package` PASS.

```text
Transport Package installation: PASS (Signature: aibridge-0.7.0)
MODX runtime verification: PASS
OK (139 tests, 736 assertions)
== 10b. TypeScript SDK live HTTP E2E ==     TYPESCRIPT SDK LIVE HTTP: PASS
PACKAGE REPRODUCIBILITY: PASS (aibridge-0.7.0, sha256 63df401306df5765c40f4a055664b08ac9239108f378f23ac75a17398f0c84a7)
STABLE certification gates passed.
```

After install all 20 `aibridge_*` rows exist in `modx_system_settings` (`aibridge_version = 0.7.0`), and the
full PHPUnit run is `OK (139 tests, 736 assertions)`.

## Recorded metadata

| field | value |
| --- | --- |
| component | modx-ai-bridge |
| version | 0.7.0 |
| release | (none — Stable) |
| package | aibridge-0.7.0.transport.zip |
| sha256 | `63df401306df5765c40f4a055664b08ac9239108f378f23ac75a17398f0c84a7` |
| size_bytes | 145536 |
| signed | false (no signing key; integrity via SHA-256) |
| reproducible | yes (local build and CI artifact hash identically) |
| modx_version | 3.2.2-pl |
| php_version | 8.2.33 |
| migration_level | 001_initial.sql, 002_approval_workflow.sql |

Evidence artifact: `release-evidence-82e9917ddd6f10a77ab9d7468435deb085fedbaa` (90 days). Reproduce with
`MODX_ROOT=/var/www/html php scripts/verify-package-reproducibility.php`.

## Status

**Stable.** Supersedes `0.6.0` (`docs/release/0.6.0.md`), `0.5.0`, `0.4.0`, `0.3.0`, `0.2.0`, `0.1.3`,
`0.1.2`, `0.1.1` and `0.1.0`. Evidence: `docs/release/stable-status.md`,
`docs/testing/iteration-57-readback-polish.md`, `docs/testing/STATUS.md`.
