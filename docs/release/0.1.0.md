# Stable Release — 0.1.0

Release: `aibridge-0.1.0.transport.zip`
Tag: `v0.1.0`
Built by: `scripts/release-candidate.php` (invoked in the MODX container with `MODX_ROOT=/var/www/html`)

## Certification

Single-command gate, executed on GitHub Actions `ubuntu-latest` (Docker + PHP 8.2 + Composer + Node.js):

```text
AIBRIDGE_RUNTIME=1 bash scripts/certification/stable-gate.sh
...
TYPESCRIPT SDK: PASS
RELEASE: aibridge-0.1.0
  package: aibridge-0.1.0.transport.zip
== MODX INTEGRATION: PASS ==
STABLE certification gates passed.
```

- First green (TypeScript gate cleared): commit `642c681`, run `34715583238`.
- 0.1.0 package certification: commit `160a659`, run `34716441523`.
- Tag-commit certification: commit `969ffb9`, run `34717021035`.
- `Quality Gates` and `MODX Integration` push runs are green for `642c681`, `160a659` and `969ffb9`.

## Recorded metadata (retained artifact)

| field | value |
| --- | --- |
| component | modx-ai-bridge |
| version | 0.1.0 |
| release | (none — Stable) |
| package | aibridge-0.1.0.transport.zip |
| sha256 | `859c659994e9b3ef41759e9df4aeeee98b795fbf613e27541d730db82e377340` |
| size_bytes | 140348 |
| signed | false (no signing key; integrity via SHA-256) |
| modx_version | 3.2.2-pl |
| php_version | 8.2.33 |
| migration_level | 001_initial.sql, 002_approval_workflow.sql |
| built_at | 2026-09-12T20:34:05+00:00 |

The SHA-256 identifies this build instance; transport zip archives are not byte-reproducible because they
embed file timestamps (CI builds of the same commit produce different hashes). Regenerate metadata with
`scripts/release-candidate.php` for any new artifact. The retained archive, its `.release.json` and
`.sha256` are attached to the `v0.1.0` GitHub Release.

Package contents verified before hashing: the archive contains `aibridge/src/Application/Application.php`,
`aibridge/api/index.php` and `aibridge/js/manager.js`.

## Status

**Stable.** Supersedes Release Candidate `0.1.0-rc1` (`docs/release/0.1.0-rc1.md`). Evidence:
`docs/release/stable-status.md`, `docs/testing/iteration-40-typescript-sdk-unblock.md`,
`docs/testing/iteration-41-stable-release.md`, `docs/testing/STATUS.md`.
